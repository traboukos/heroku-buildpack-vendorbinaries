#!/bin/bash

# Downloading spark
cd /app
wget http://d3kbcqa49mib13.cloudfront.net/spark-2.1.0.tgz
tar -zxf spark-2.1.0.tgz
cd spark-2.1.0

CACHE_DIR=$2
ls $CACHE_DIR/apt/cache

# Building spark
sbt assembly
