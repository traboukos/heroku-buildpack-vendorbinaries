#!/bin/bash

BUILD_DIR=$1 

# Downloading spark
cd /app
wget http://d3kbcqa49mib13.cloudfront.net/spark-2.1.0.tgz
tar -zxf spark-2.1.0.tgz
cd spark-2.1.0

ls -lah $BUILD_DIR/.apt/usr/bin

# Building spark
$BUILD_DIR/.apt/usr/bin/sbt assembly
